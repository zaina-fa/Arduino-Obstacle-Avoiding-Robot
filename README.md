# Arduino-Obstacle-Avoiding-Robot
his project is an Arduino-based obstacle avoiding robot that uses an HC-SR04 ultrasonic sensor to detect objects and automatically control two DC motors to avoid collisions. The robot continuously measures the distance in front of it and changes its movement when an obstacle is detected.
